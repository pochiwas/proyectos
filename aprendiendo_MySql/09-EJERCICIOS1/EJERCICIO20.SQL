/*SELECCIONAR EL GRUPO EN EL QUE TRABAJA
EL VENDEDOR CON MAYOR SUELDO Y MOSTRAR EL NOMBRE DEL GRUPO*/

SELECT *
FROM GRUPO 
WHERE ID IN (SELECT GRUPO_ID
             FROM VENDEDOR 
             WHERE SUELDO = (SELECT MAX(SUELDO)
                             FROM VENDEDOR));