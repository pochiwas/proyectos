/*OBTENER LOS VENDEDORES CON 2 O MAS CLIENTES*/

SELECT * 
FROM VENDEDOR
WHERE ID IN (SELECT VENDEDOR_ID 
                     FROM CLIENTE
                     GROUP BY VENDEDOR_ID
                     HAVING COUNT(ID) >=2);


SELECT * FROM CLIENTE;
