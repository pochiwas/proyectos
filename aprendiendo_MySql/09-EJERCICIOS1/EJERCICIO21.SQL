/*OBTENER NOMBRES Y CIUDADES DE LOS
CLIENTES QUE TIENEN ENCARGOS
QUE SEAN MAYORES O IGUALES A 2*/

SELECT NOMBRE,CIUDAD
FROM CLIENTE
WHERE ID IN (SELECT CLIENTE_ID 
             FROM ENCARGO
             WHERE ID = CLIENTE_ID
             AND CANTIDAD >=2);