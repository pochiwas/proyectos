/*OBTENER LISTA DE NOMBRE DE CLIENTES
CON EL IMPORTE(LO QUE HAN GASTADO) DE SUS ENCARGOS ACUMULADOS*/

SELECT C.NOMBRE,SUM(PRECIO*CANTIDAD) AS 'IMPORTE'
FROM CLIENTE C
INNER JOIN ENCARGO E ON C.ID = E.CLIENTE_ID
INNER JOIN COCHE CO ON CO.ID = E.COCHE_ID
GROUP BY C.NOMBRE;

